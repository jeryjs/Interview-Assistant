<Window x:Class="Naveen_Sir.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Interview Helper"
        Height="760"
        Width="1140"
        MinHeight="620"
        MinWidth="940"
        WindowStartupLocation="CenterScreen"
        Background="#0C0F14">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#F2F4F8" />
            <Setter Property="Background" Value="#222A38" />
            <Setter Property="BorderBrush" Value="#38465E" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="ToggleButton">
            <Setter Property="Foreground" Value="#E6ECF7" />
            <Setter Property="Background" Value="#1A2331" />
            <Setter Property="BorderBrush" Value="#38465E" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="0,0,8,0" />
        </Style>
        <Style x:Key="ChipButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="Background" Value="#263752" />
            <Setter Property="BorderBrush" Value="#4E6D98" />
            <Setter Property="Padding" Value="12,7" />
        </Style>
    </Window.Resources>

    <Border Margin="10" CornerRadius="12" Background="#111823" BorderBrush="#273247" BorderThickness="1">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,10" Background="#0F1621" MouseLeftButtonDown="OnDragHandleMouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Interview Assistant" Foreground="#EFF4FF" FontSize="19" FontWeight="SemiBold" Margin="10,8,12,8" />
                    <TextBlock x:Name="ProviderStatusText" Foreground="#8FA8CC" VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock x:Name="StatusText" Grid.Column="1" Foreground="#9CB4D6" Margin="0,8,10,8" VerticalAlignment="Center" />
            </Grid>

            <WrapPanel Grid.Row="1" Margin="0,0,0,10">
                <ToggleButton x:Name="MicToggle" Content="Mic" Checked="OnCaptureToggleChanged" Unchecked="OnCaptureToggleChanged" />
                <ToggleButton x:Name="SystemToggle" Content="System Audio" Checked="OnCaptureToggleChanged" Unchecked="OnCaptureToggleChanged" />
                <ToggleButton x:Name="ScreenToggle" Content="Screen Share" Checked="OnCaptureToggleChanged" Unchecked="OnCaptureToggleChanged" />
                <Button Content="Provider Settings" Click="OnOpenSettings" />
                <Button Content="Clear Chips" Click="OnClearChips" />
            </WrapPanel>

            <Grid Grid.Row="2" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>

                <GroupBox Header="Live Transcript" Foreground="#E6ECF7" Margin="0,0,10,0">
                    <ListBox x:Name="TranscriptList" Background="#0A111B" Foreground="#D2DDF0" BorderBrush="#25324A" BorderThickness="1" />
                </GroupBox>

                <GroupBox Grid.Column="1" Header="Topic Recommendations" Foreground="#E6ECF7">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ListBox x:Name="ChipList" Background="#0A111B" Foreground="#D2DDF0" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource ChipButtonStyle}" Click="OnChipClick" Content="{Binding Text}" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </ScrollViewer>
                </GroupBox>
            </Grid>

            <GroupBox Grid.Row="3" x:Name="ThreadPanel" Header="Topic Thread" Foreground="#E6ECF7" Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="ThreadTitleText" Foreground="#BFD5F6" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <Button Grid.Column="1" Content="Close Thread" Click="OnCloseThread" Margin="0" />
                    </Grid>
                    <TextBox x:Name="ThreadContentBox"
                             Grid.Row="1"
                             FontFamily="Consolas"
                             FontSize="13"
                             Background="#0A111B"
                             Foreground="#DCE8FF"
                             BorderBrush="#25324A"
                             BorderThickness="1"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled" />
                </Grid>
            </GroupBox>
        </Grid>
    </Border>
</Window>
